<p id="notice"><%= notice %></p>

<div class="my-3 p-3 bg-white rounded shadow-sm">

  <h1 class="border-bottom border-gray pb-2 mb-0 text-center my-3">Avisos recientes</h1>

  <% @ads.each do |ad| %>

  <div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <h4 class="card-title"><%= ad.category%>.</h4>
          <h3><% ad.tags.each do |tag| %>
          [<%= tag.title %>]
          <% end %>
          </h3>
          <h5><%= ad.title %>.</h5>
        </div>
        <div class="card-action">
          <% if can? :manage, ad %>
        <td><%= link_to 'Editar', edit_ad_path(post), class: 'btn btn-sm btn-success' %></td>
        <% end %>
        <%if can? :manage, ad%>
        <td><%= link_to 'Eliminar', ad, method: :delete, data: { confirm: 'Estás seguro que deseas eliminar el Post?' }, class: 'btn btn-sm btn-danger' %></td>
        
        <% end %>
      </div>
    </div>
  </div>

  <% end %>
